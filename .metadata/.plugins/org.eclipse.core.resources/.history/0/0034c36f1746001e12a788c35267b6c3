package PalindromeNumber;
public class CheckingPalindrome {
	void palindrome(int n)
	{
		int org=n;
		int rev=0;
		while(n>0)
		{
			int ld=n%10;
			 rev=ld*10+ld;
			 n=n/10;
		
		}
		System.out.println(rev);
	
	}

}
